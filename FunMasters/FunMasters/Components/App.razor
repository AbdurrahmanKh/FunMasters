<!DOCTYPE html>
<html lang="en" data-theme="fun-dark" data-theme-dark="fun-dark">
    <head>
        <meta charset="utf-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <meta name="darkreader-lock"/>
        <base href="/"/>
        <link rel="preconnect" href="https://fonts.googleapis.com"/>
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
        <link href="https://fonts.googleapis.com/css2?family=EB+Garamond:ital,wght@0,400..800;1,400..800&display=swap" rel="stylesheet"/>
        <link rel="stylesheet" href="@Assets["css/site.css"]"/>
        <ImportMap/>
        <link rel="icon" type="image/png" href="favicon.png"/>
        <HeadOutlet @rendermode="PageRenderMode"/>
    </head>

    <body class="pb-10 min-h-screen flex flex-col">
        <Routes @rendermode="PageRenderMode"/>

        @* Without this element Blazor has nowhere to surface a client-side failure, so the page
           just stops responding with no message anywhere. *@
        <div id="blazor-error-ui">
            <span class="font-bold">Something went wrong.</span>
            <span id="blazor-error-detail"></span>
            <a href="." class="reload underline ms-2">Reload</a>
            <span class="dismiss">✕</span>
        </div>

        <script src="_framework/blazor.web.js"></script>
    </body>
</html>

@code {
    [CascadingParameter] private HttpContext HttpContext { get; set; } = default!;

    private IComponentRenderMode? PageRenderMode =>
        HttpContext.AcceptsInteractiveRouting() ? InteractiveWebAssembly : null;

}